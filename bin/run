#!/usr/bin/env ruby  #shebang line tells terminal to intreprete the ruby interpreter language 

require_relative '../lib/got_cli.rb' 

Cli.new.run # new instances, calling on run, calling and starting applcation
